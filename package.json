{
  "name": "listmaster-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "packageManager": "npm@11.12.1",
  "devDependencies": {
    "turbo": "^2.9.12"
  }
}
